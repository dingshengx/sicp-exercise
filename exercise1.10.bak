#lang sicp
#|
练习 1.10:以下步骤计算称为阿克曼函数的数学函数。

(define (A x y)
  (cond ((= y 0) 0)
        ((= x 0) (* 2 y))
        ((= y 1) 2)
        (else (A (- x 1)
                 (A x (- y 1))))))
以下表达式的值是多少?

(A 1 10)
(A 2 4)
(A 3 3)
考虑以下过程,其中 A 是上面定义的过程:

(define (f n) (A 0 n))
(define (g n) (A 1 n))
(define (h n) (A 2 n))
(define (k n) (* 5 n n))
给出由过程 f 、 g 和 h 计算的函数的简明数学定义,其中 n
 为正整数值。例如, (k n) 计算 5n2
 。
|#